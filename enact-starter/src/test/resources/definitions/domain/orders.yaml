use-cases:
  createOrder:
    description: Stores a new order
    group: orderAdmin
    trigger:
      rest:
        method: POST
        path: /api/v1/orders
        bind:
          createdBy: header:X-Created-By
    steps:
      - step: saveOrder

groups:
  orderAdmin:
    filters: [audit]
